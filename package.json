{
  "name": "dstats-vercel",
  "version": "1.0.0",
  "description": "Real-time request analytics dashboard",
  "main": "api/stats.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
